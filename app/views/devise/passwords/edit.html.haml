%h2= t('devise.passwords.change')
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
  = render "devise/shared/error_messages", resource: resource
  = f.hidden_field :reset_password_token
  .field
    = f.label :password, t('devise.passwords.new')
    %br/
    - if @minimum_password_length
      %em
        (#{@minimum_password_length} characters minimum)
      %br/
    = f.password_field :password, autofocus: true, autocomplete: t('devise.passwords.new')
  .field
    = f.label :password_confirmation, t('devise.passwords.confirm')
    %br/
    = f.password_field :password_confirmation, autocomplete: t('devise.passwords.new')
  .actions
    = f.submit t('devise.passwords.change')
= render "devise/shared/links"
