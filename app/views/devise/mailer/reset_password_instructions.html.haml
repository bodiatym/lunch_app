%p
  = t('hello')
  = @resource.email
  \!
%p= t('devise.passwords.change')
%p= link_to t('devise.passwords.change'), edit_password_url(@resource, reset_password_token: @token)
%p= t('devise.mailer.ignore')
