<p> <%= t('welcome ')%><%= @email %>!</p>

<p><%=t('devise.confirmations.email.confirm_link') %></p>

<p><%= link_to t('devise.confirmations.confirm_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
