%h1=t('.profile', name: @profile.user.name)
%li= link_to t('edit'), edit_users_profile_path(current_user.profile.id), class: "btn btn-primary btn-sm mt-10"
%li= link_to t('home'), home_path, class: "btn btn-primary btn-sm mt-10"
